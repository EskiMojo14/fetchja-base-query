# fetchja-base-query

An RTK Query `baseQuery` backed by the [fetchja](https://fetchja.dev/) JSON:API client.

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
